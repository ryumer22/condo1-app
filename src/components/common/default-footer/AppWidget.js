import React from "react";

const AppWidget = () => {
  return (
    <div className="app-widget" style={{ minHeight: "50px" }}>
      {/* intentionally left blank */}
    </div>
  );
};

export default AppWidget;
